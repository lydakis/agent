{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "The page may use the core APIs (events, its own window) and the app's commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
